The model trains without any errors but the validation accuracy is really underwhelming. It starts okay-ish for the first couple epochs but then it just kind of stalls out and doesn't improve much. Training accuracy keeps going up a bit but val accuracy stays stuck around 40-50%. I expected it to get much higher since the dataset isn't that complicated. The learning rate reducer kicks in pretty early too which seems weird. Not sure what's going on.
